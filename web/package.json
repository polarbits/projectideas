{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@syncano/client": "^0.8.0-alpha.617377ac",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "syncano-update": "npm install -g @syncano/cli@canary; yarn add @syncano/client@canary"
  }
}
